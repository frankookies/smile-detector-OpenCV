# smile-detector-OpenCV

Using Haar cascade classifiers provided by OpenCV to build a simple real time face and smile detector.

### Deliverables:

- Python script on Jupyter notebook (midterm_smile_detector.ipynb)
- Output video (detection_example.mov)
- Some saved image samples (smiles folder)
